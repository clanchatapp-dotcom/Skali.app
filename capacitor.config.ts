import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'app.skali.mobile',
  appName: 'Skali',
  webDir: 'dist',
  server: { androidScheme: 'https' },
}

export default config
